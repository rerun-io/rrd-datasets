"""
Modal jobs for the LIBERO hdf5 to rrd conversion.

`convert_tasks` defines the job. The dataset-independent helpers live in `rrd_datasets_common.modal_jobs`.
"""

from __future__ import annotations
